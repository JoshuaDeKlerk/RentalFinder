{
  "name": "rentalfinder",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix rentalfinder && npm run build --prefix rentalfinder",
    "start": "npm install --prefix backend && npm start --prefix backend"
  },
  "dependencies": {
    "rentalfinder": "file:"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
